const root=document.documentElement;const dataUrl=root.dataset.eventsData;const archiveMode=root.dataset.archive==='true';const state={category:'all',area:'all',status:'all',free:false,search:'',sort:'start'};let events=[];const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));function dateValue(v){return new Date(v+'T00:00:00+09:00').getTime()}function updateMetrics(){const art=events.filter(e=>e.category==='art').length;const anime=events.filter(e=>e.category==='anime').length;const upcoming=events.filter(e=>e.status==='upcoming').length;$('#metric-total').textContent=events.length;$('#metric-art').textContent=art;$('#metric-anime').textContent=anime;$('#metric-upcoming').textContent=upcoming}function card(e){return `<article class="card"><div class="card-top"><div class="badges"><span class="badge">${esc(e.categoryLabel)}</span><span class="badge ${esc(e.status)}">${esc(e.statusLabel)}</span></div><span class="muted">${esc(e.area)}</span></div><h3>${esc(e.zh)}</h3><div class="jp">${esc(e.title)}</div><p class="desc">${esc(e.description)}</p><dl class="meta"><dt>期間</dt><dd>${esc(e.period)}</dd><dt>地點</dt><dd>${esc(e.venue)}</dd><dt>交通</dt><dd>${esc(e.station)}</dd><dt>費用</dt><dd>${esc(e.price)}</dd><dt>預約</dt><dd>${esc(e.reservation)}</dd></dl><div class="links"><a class="link primary" href="${esc(e.official)}" target="_blank" rel="noopener noreferrer">官方網站 ↗</a><a class="link secondary" href="${esc(e.reference)}" target="_blank" rel="noopener noreferrer">參考來源 ↗</a></div></article>`}function render(){let result=events.filter(e=>{const hay=`${e.title} ${e.zh} ${e.description} ${e.area} ${e.venue} ${e.station}`.toLowerCase();return(state.category==='all'||e.category===state.category)&&(state.area==='all'||e.area===state.area)&&(state.status==='all'||e.status===state.status)&&(!state.free||e.free)&&(!state.search||hay.includes(state.search.toLowerCase()))});result.sort((a,b)=>state.sort==='end'?dateValue(a.end)-dateValue(b.end):state.sort==='area'?a.area.localeCompare(b.area,'zh-Hant'):dateValue(a.start)-dateValue(b.start));$('#count').textContent=`顯示 ${result.length}／${events.length} 項`;$('#list').innerHTML=result.map(card).join('');$('#empty').style.display=result.length?'none':'block'}function bind(){ $$('.tab').forEach(btn=>btn.addEventListener('click',()=>{state.category=btn.dataset.category;$$('.tab').forEach(x=>x.classList.toggle('active',x===btn));render()}));$('#search').addEventListener('input',e=>{state.search=e.target.value.trim();render()});$('#area').addEventListener('change',e=>{state.area=e.target.value;render()});$('#status').addEventListener('change',e=>{state.status=e.target.value;render()});$('#free').addEventListener('change',e=>{state.free=e.target.checked;render()});$('#sort').addEventListener('change',e=>{state.sort=e.target.value;render()})}async function init(){try{const res=await fetch(dataUrl,{cache:'no-store'});if(!res.ok)throw new Error(`HTTP ${res.status}`);const data=await res.json();events=data.events||[];$('#updated').textContent=`資料更新：${data.updated}`;$('#title').textContent=data.title+(archiveMode?'｜歷史存檔':'');updateMetrics();bind();render()}catch(err){$('#list').innerHTML=`<div class="notice">資料載入失敗：${esc(err.message)}。請重新整理頁面。</div>`;$('#count').textContent='載入失敗'}}init();